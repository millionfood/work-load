<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
    xmlns:tx="http://www.springframework.org/schema/tx"
    xsi:schemaLocation="http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
        http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
        http://www.springframework.org/schema/tx https://www.springframework.org/schema/tx/spring-tx.xsd">
<!-- 파일을 읽어와서 값을 꺼내 쓸 수 있게 해준다. - 사용규칙은 없고 꺼내쓸 때 안에 있는 변수명과 일치하기만 하면 됨 -->
    <context:property-placeholder location="classpath:db.properties"/>
<!-- 데이터베이스와 연결되는 통로를 만드는 객체 -->
    <bean id="dataSource" class="org.mariadb.jdbc.MariaDbPoolDataSource" destroy-method="close">
        <property name="url" value="${db.url}"/>
        <property name="user" value="${db.user}"/>
        <property name="password" value="${db.password}"/>
        <property name="maxPoolSize" value="10"/> 
    </bean>
<!-- MyBatis가 SQL을 실행할 때 사용하는 가장 중요한 객체, 위에서 만든 dataSource를 가져와서 연결 -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
    </bean>
<!-- 해당 패키지 안에 있는 클래스들 중 @Component 계열의 어노테이션을 자동으로 찾아 등록 -->
    <context:component-scan base-package="com.fmc.service"/>
<!-- 해당 패키지 않에 있는 인터페이스들은 자동으로 MyBatis Mapper로 등록 -->
    <mybatis-spring:scan base-package="com.fmc.mapper"/>
<!-- 트랜잭션을 관리, dataSource(데이터베이스 연결)을 꽉 쥐고 있으면서 SQL작업이 시작될 때, 트랜잭션 시작, 작업 끝나면 커밋,롤백 등을 수행 -->
    <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>
<!-- 트랜잭션을 관리, @Transactional 어노테이션으 붙으면 transactionalManager 을 사용하도록 설정-->
    <tx:annotation-driven/>

</beans>