<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee 
                             http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">
<!-- context-param : 프로젝트 전체 공용 설정, 웹 애플리케이션 전체가 공유할 수 있는 전역 변수를 선언하는 곳 -->
    <context-param>
	<!-- 이 param-name은 스프링에서 root-context.xml을 가르키기로 약속된 이름 (정해진 이름) -->
        <param-name>contextConfigLocation</param-name>
        <param-value>/WEB-INF/spring/root-context.xml</param-value>
    </context-param>
<!-- 서버가 켜질 때 context-param을 읽어서 거대한 기계를 초기화 하는 역할, 없으면 스프링 설정 파일이 읽혀지지 않음 -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>
<!-- 사용자 정의 리스너 -->    
    <listener>
        <listener-class>com.pj.listener.AppListener</listener-class>
    </listener>
<!-- 모든 필터는 filter,filter-mapping이 한 세트 -->
<!-- filter : 어떤 클래스(부품)를 필터로 쓸 것인가를 정의 -->
<!-- filter-mapping : 그 필터를 어떤 주소(URL)에 적용할 것인가를 정의 -->
<!-- 중요한것은 encoding 필터가 가장 위에 있어야 한다는것, 즉 필터들 사이의 순거가 매우 중요함 -->
    <filter>
        <filter-name>encoding</filter-name>
        <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>encoding</filter-name>
        <!-- 필터가 실행될 주소(모든 요청 : /*) -->
        <url-pattern>/*</url-pattern>
    </filter-mapping>
<!-- filter를 통과한 요청을 어디로 안내할지 결정하는 곳 -->
    <servlet>
    <!-- servlet-name : 서블릿의 별명, 나중에 servlet-mapping에서 이 이름을 불러서 연결 -->
        <servlet-name>appServlet</servlet-name>
    <!-- 스프링이 제공하는 DispatcherServlet이라는 클래스를 가져다 쓰겠다는 뜻 DispatcherServlet - 스프링 MVC의 심장 -->
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
	    <!-- context-param의 param-name과 이름이 같지만 범위가 달라서 다른 용도로 사용됨 -->
	    <!-- appServlet만 사용하는 지침서인 servlet-context.xml의 위치를 알려준다. (정해진 이름) -->
            <param-name>contextConfigLocation</param-name>
            <param-value>/WEB-INF/spring/appServlet/servlet-context.xml</param-value>
        </init-param>
    <!-- 서버가 켜질 때 서블릿을 몇번째 순서로 미리 메모리에 올릴 것인가. (보통 1부터 시작, 0도 가능하지만 관례적으로 가장 중요한 녀석에게 1) -->
        <load-on-startup>1</load-on-startup>
    <!-- 이 서블릿은 파일 업로드(Multipart)요청을 처리할 수 있다는 허가증 -->
        <multipart-config/>
    </servlet>
<!-- servlet에서 만든 servlet에 해당하는 URL을 배정 -->
    <servlet-mapping>
        <servlet-name>appServlet</servlet-name>
        <url-pattern>/</url-pattern>
    </servlet-mapping>

</web-app>